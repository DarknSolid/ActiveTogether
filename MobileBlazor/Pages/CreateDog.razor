@using MobileBlazor.Extensions
@attribute [Route(RoutingConstants.CREATE_DOG + "{DogId:int?}")]
@inject NavigationManager NavigationManager;
@inject IJSRuntime JSRuntime;

<RazorLib.Pages.DogCreation DogId="DogId" OnDogCreationFinished="@(() => NavigationManager.GoBack(JSRuntime))"></RazorLib.Pages.DogCreation>

@code {
    [Parameter]
    public int DogId { get; set; }
}
